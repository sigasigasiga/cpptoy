{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "(vsc) Launch file",
            "program": "${workspaceFolder}/bin/toy.exe",
            "args": "",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}
